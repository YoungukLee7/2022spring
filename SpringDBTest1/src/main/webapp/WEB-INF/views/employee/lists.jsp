<%@ page language="java" contentType="text/html; charset=EUC-KR"
    pageEncoding="EUC-KR"%>
<%@ taglib prefix="c" uri="http://java.sun.com/jsp/jstl/core" %>
<!DOCTYPE html>
<html>
<head>
<meta charset="EUC-KR">
<title>employee list</title>
</head>
<body>

		<h3>employee list</h3>
<!-- 		
		<table border='1'>
		
			<c:forEach items="${list }" var="lists">
				
				<tr>
					<td>${lists.employee_id }</td>
					<td>${lists.first_name }</td>
					<td>${lists.last_name }</td>
					<td>${lists.email }</td>
					<td>${lists.phone_number }</td>
					<td>${lists.hire_date }</td>
					<td>${lists.job_id }</td>
					<td>${lists.salary }</td>
					<td>${lists.commission_pct }</td>
					<td>${lists.manager_id }</td>
					<td>${lists.department_id }</td>
					<td><a href="updates?id=${lists.employee_id }">수정</a></td>
					<td><a href="deletes?id=${lists.employee_id }">삭제</a></td>
				</tr>
			</c:forEach>
		</table>
 -->	
</body>
</html>